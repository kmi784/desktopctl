import argparse

# listings

# control

# public API ---------------------------------------------------------------------------


def configure_bluetooth_parser(parser: argparse.ArgumentParser) -> None:
    """Configure the Bluetooth CLI commands.

    Parameters
    ----------
    `parser` : `argparse.ArgumentParser`
        Bluetooth parser of `desktopctl`
    """
